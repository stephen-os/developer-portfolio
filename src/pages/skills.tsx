import Layout from '@/pages/layout';

import SkillsIndex from '@/components/pages/skills/index';

export default function Skills() {
    return (
        <Layout>
            <SkillsIndex />
        </Layout>
    );
}