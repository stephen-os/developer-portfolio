import Layout from '@/pages/layout';

import ContactIndex from '@/components/pages/contact/index';

export default function Skills() {
    return (
        <Layout>
            <ContactIndex />
        </Layout>
    );
}